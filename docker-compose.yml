services:
  brand-prompt-compare:
    build: .
    container_name: brand-prompt-compare
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      # Persist the local SQLite DB (and keep existing history) on the host.
      - ./data:/app/data
